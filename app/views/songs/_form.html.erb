<%= form_for(song) do |f| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :song_name %>
    <%= f.text_field :song_name %>
  </div>

  <div class="field">
    <%= f.label :song_order %>
    <%= f.number_field :song_order %>
  </div>

  <div class="field">
    <%= f.label :song_duration %>
    <%= f.text_field :song_duration %>
  </div>

  <div class="field">
    <%= f.label :album_id %>
    <%= f.text_field :album_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
